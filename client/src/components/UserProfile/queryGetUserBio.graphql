query GetUserBio($id: ID!){
	getUser(id: $id){
		firstName,
    lastName,
    email,
    language,
    birthday,
    gender,
    bio,
    events{
      id
    },
    groups{
      id
    },
    enabled,
    createdAt
	}
}

